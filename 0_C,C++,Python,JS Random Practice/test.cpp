#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int x, y;
    cout << "Enter two numbers: ";
    cin >> x >> y;
    cout << "the sum is: " << x + y << "\n";
}
